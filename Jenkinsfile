pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "Build step here"
            }
        }

        stage('Deploy to Web Server') {
            steps {
                echo "Deploy step here"
            }
        }
    }
}
